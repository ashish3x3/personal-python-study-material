try:
                # raise Exception("I know python!")

                current_datetime = datetime.strptime(date, "%Y-%m-%d")
            except Exception, e:
                exc_type, exc_value, exc_traceback = sys.exc_info()
                urls = [settings.LOGGER +'/log']
                print url
